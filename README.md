# Lolz Deals bot
t.me/skladmaterialov

## Установка

```bash
python -m venv .venv
.venv\Scripts\activate
pip install -r requirements.txt
```

## Настройка

Токен передаётся при запуске (ничего не сохраняется в файлах):

## Запуск

```bash
python main.py <BOT_TOKEN>
```

## Что сделано

- `/start` отправляет `forbot.jpg` с HTML-текстом (bold + blockquote + premium emoji-id)
- Инлайн-кнопки с `icon_custom_emoji_id` (как в твоём примере)

